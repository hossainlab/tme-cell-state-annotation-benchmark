name: tme_benchmark
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - pip
  - pyyaml
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - seaborn
  - pip:
      - scanpy
      - scvi-tools
      - celltypist
      - scrublet
      - harmonypy
      - anndata
      - zellkonverter   # AnnData <-> SingleCellExperiment handoff for the R tools
# scGPT installs separately (heavy, needs torch + flash-attn):
#   pip install scgpt
